--- modules/afridebug/src/debug/integration/execution/AfriDebugRepairExecutionBridge.js	2026-08-08 10:03:19.758317499 +0100
+++ src/afridebug/platform/integration/execution/AfriDebugRepairExecutionBridge.js	2026-08-09 18:01:07.517330957 +0100
@@ -1,88 +1,91 @@
-import Planner from "../../planning/AfriDebugRepairPlanningEngine.js";
 import Controller from "../../execution/AfriDebugChangeExecutionController.js";
-import Approval from "../../approval/AfriDebugRepairApprovalQueue.js";
-import History from "../../history/AfriDebugRepairHistoryLedger.js";
-import Resolution from "../../learning/AfriDebugResolutionRecorder.js";
+import History from "../../../services/history/AfriDebugRepairHistoryLedger.js";
+import Resolution from "../../../services/learning/AfriDebugResolutionRecorder.js";
 
 const AfriDebugRepairExecutionBridge = {
 
   execute(input={}){
 
-    const approval =
-      Approval.list().find(
-        item => item.approvalId === input.approvalId
-      );
+    const execution =
+      Controller.execute(input);
+
+    if(execution.status !== "completed"){
 
-    if(!approval || approval.status !== "approved"){
       return {
-        bridgeStatus:"blocked",
-        reason:"APPROVAL_REQUIRED"
+        bridgeStatus:"failed",
+        execution,
+        history:null,
+        resolution:null
       };
-    }
-
-    const plan =
-      Planner.plan({
-        issue:input.issue,
-        files:input.files || [],
-        risk:input.risk
-      });
 
+    }
 
-    const execution =
-      Controller.execute({
+    const history =
+      History.record({
 
         incidentId:
-          input.incidentId,
+          input.incidentId || execution.incidentId,
+
+        approvalId:
+          input.approvalId || null,
 
         issue:
-          plan.issue,
+          input.issue || null,
+
+        diagnosis:
+          input.diagnosis || null,
+
+        planId:
+          input.planId || null,
 
-        files:
-          plan.files,
+        executionId:
+          execution.executionId,
 
-        action:
-          plan.action,
+        verificationStatus:
+          execution.verification?.status || "unknown",
 
-        tests:
-          input.tests || [],
+        rollbackStatus:
+          execution.rollback
+            ? "required"
+            : "none",
 
-        version:
-          input.version || "v1.0"
+        outcome:
+          execution.status
 
       });
 
+    const resolution =
+      Resolution.record({
 
-    const history = History.record({
-      incidentId: input.incidentId || execution.incidentId,
-      approvalId: input.approvalId,
-      issue: plan.issue,
-      diagnosis: plan.diagnosis,
-      planId: plan.planId,
-      executionId: execution.executionId,
-      verificationStatus: execution.verification?.status,
-      rollbackStatus: execution.rollback ? "required" : "none",
-      outcome: execution.status
-    });
-
-    const resolution = Resolution.record({
-      investigationId: execution.incidentId,
-      error: plan.issue,
-      diagnosis: plan.diagnosis,
-      patch: execution.patch,
-      status: execution.status
-    });
+        investigationId:
+          input.incidentId || execution.incidentId,
+
+        error:
+          input.issue || null,
+
+        diagnosis:
+          input.diagnosis || null,
+
+        patch:
+          execution.patch || input.patch || null,
+
+        deliveryId:
+          input.deliveryId || null,
+
+        status:
+          execution.status
+
+      });
 
     return {
 
-      plan,
+      bridgeStatus:"executed",
 
       execution,
 
       history,
 
-      resolution,
-
-      bridgeStatus:"executed"
+      resolution
 
     };
 
@@ -93,9 +96,14 @@
 
     return {
 
-      service:"AfriDebugRepairExecutionBridge",
+      service:
+        "AfriDebugRepairExecutionBridge",
 
-      status:"healthy"
+      status:
+        "healthy",
+
+      responsibility:
+        "governed-repair-execution-and-post-execution-recording"
 
     };
 
@@ -103,5 +111,4 @@
 
 };
 
-
 export default AfriDebugRepairExecutionBridge;
