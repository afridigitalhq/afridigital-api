export const AFRI_PREDICTION_OUTCOMES=Object.freeze(["home","draw","away"]);
export const AFRI_PREDICTION_MARKETS=Object.freeze(["match_prediction","correct_score","over_under","btts","double_chance","odds","xg"]);
export function createAfriPrediction({fixtureId,league,season,homeTeam,awayTeam,provider,probabilities={},prediction=null,correctScore=null,expectedGoals=null,markets={},metadata={}}={}){if(!fixtureId)throw new Error("Prediction fixtureId is required");if(!provider)throw new Error("Prediction provider is required");return Object.freeze({type:"AFRI_PREDICTION",fixtureId:String(fixtureId),league:league??null,season:season??null,homeTeam:homeTeam??null,awayTeam:awayTeam??null,provider,prediction:AFRI_PREDICTION_OUTCOMES.includes(prediction)?prediction:null,probabilities:Object.freeze({home:Number(probabilities.home??0),draw:Number(probabilities.draw??0),away:Number(probabilities.away??0)}),correctScore:correctScore??null,expectedGoals:expectedGoals===null?null:Number(expectedGoals),markets:Object.freeze({...markets}),metadata:Object.freeze({...metadata})})}
