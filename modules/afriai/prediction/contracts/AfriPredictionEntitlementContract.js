export const AFRI_PREDICTION_ACCESS_MODES=Object.freeze(["free","paid","payg","subscription"]);
export function createAfriPredictionEntitlement({mode="free",enabled=true,credits=0,subscription=null,markets=[]}={}){if(!AFRI_PREDICTION_ACCESS_MODES.includes(mode))throw new Error(`Unsupported prediction access mode: ${mode}`);return Object.freeze({type:"AFRI_PREDICTION_ENTITLEMENT",mode,enabled:Boolean(enabled),credits:Number(credits)||0,subscription:subscription??null,markets:Object.freeze([...markets])})}
export function canUsePrediction(entitlement={}){if(entitlement.enabled===false)return false;if(entitlement.mode==="free")return true;if(entitlement.mode==="subscription")return Boolean(entitlement.subscription);if(entitlement.mode==="payg"||entitlement.mode==="paid")return Number(entitlement.credits)>0;return false}
