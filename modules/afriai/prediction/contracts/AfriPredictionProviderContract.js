export const AFRI_PREDICTION_CAPABILITIES=Object.freeze(["match_prediction","probabilities","correct_score","over_under","goals","btts","odds","xg","form"]);
export function createAfriPredictionProviderContract({name,capabilities=[],coverage="global",predictMatch,health,scaffoldOnly=false}){if(!name)throw new Error("Afri prediction provider name is required");if(typeof predictMatch!=="function")throw new Error(`Afri prediction provider "${name}" requires predictMatch`);return Object.freeze({name,type:"AFRI_PREDICTION_PROVIDER",scaffoldOnly:Boolean(scaffoldOnly),capabilities:Object.freeze(capabilities.filter(item=>AFRI_PREDICTION_CAPABILITIES.includes(item))),coverage, supports(capability){return this.capabilities.includes(capability)},supportsLeague(league){if(this.coverage==="global")return true;if(Array.isArray(this.coverage))return this.coverage.includes(String(league??""));return false},async predictMatch(input={}){return predictMatch(input)},async health(){return typeof health==="function"?health():{status:"unknown",provider:this.name}}})}
