{
  "name": "afridigital-backend",
  "version": "1.0.0",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build:frontend": "cd ../afridigital-frontend && npm install && npm run build",
    "postinstall": "npm run build:frontend && rm -rf public/* && cp -r ../afridigital-frontend/dist/* public/"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}