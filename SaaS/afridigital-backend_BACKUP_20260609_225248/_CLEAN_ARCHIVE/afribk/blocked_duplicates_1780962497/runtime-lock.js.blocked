function enforceSingleRuntime(app) {
  const original = // app.listen DISABLED;

  // app.listen DISABLED = function () {
    console.error("🚨 RUNTIME VIOLATION: Only render-entry.js may start server");
    console.trace();
    process.exit(1);
  };

  return app;
}

module.exports = { enforceSingleRuntime };
